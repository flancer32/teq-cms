# Iteration Report

## Goal
- Mention the `.mjs` configurator preference and `.js` fallback in `README.md` so the README matches the updated documentation.

## Actions
- Explained in `README.md` that `teqcms.config.mjs` should be used first with an optional `teqcms.config.js` fallback, while package-level configurators keep the same contract.

## Resulting Artifacts
- `README.md`
