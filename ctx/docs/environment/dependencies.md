# Dependency Baseline

The dependency-switch checkpoint targets the following package generation:

- `@flancer32/teq-tmpl` `^0.4.0`.
- `@flancer32/teq-web` `^0.16.0`.
- `@teqfw/di` `^2.9.0`.
- `@teqfw/log` `^2.0.0`.
- `@teqfw/cfg` `^2.0.0`.
- `@teqfw/cli` `^2.0.0`.

The package requires Node.js 20 or newer.
The dependency graph is switched before source compatibility is restored.
